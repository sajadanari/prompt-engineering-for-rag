# Copy this file to .env and fill in your key(s).
# .env is gitignored — API keys never enter version control.

# Primary API key for the configured provider (AvalAI by default).
AVALAI_API_KEY=aa-your-key-here

# Optional fallback key, tried automatically if the primary fails auth.
AVALAI_API_KEY_FALLBACK=
